apiVersion: v1
kind: Service
metadata:
  name: <%= @service_name %>
spec:
  selector:
    instance: <%= @instance_tag %>
    type: server
  ports:
    - protocol: TCP
      port: <%= @port %>
      targetPort: 5201
